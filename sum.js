function sum(a, b) {
  return a + b;
}

/*
BREAKING CHANGE FOR THE WORKSHOP:

Later in the workshop, comment out the working function above and uncomment this one.
That should break the test stage.

function sum(a, b) {
  return a - b;
}
*/

module.exports = sum;
